interface:
  display_name: "Handle Automated Code Review"
  short_description: "Verify bot findings and safely act on them"
  default_prompt: "Use $handle-automated-code-review to verify these automated findings and recommend or apply only safe, authorized fixes."
policy:
  allow_implicit_invocation: true
