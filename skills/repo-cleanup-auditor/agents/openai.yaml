interface:
  display_name: "Repository Cleanup Auditor"
  short_description: "Audit clutter before making cleanup changes"
  default_prompt: "Use $repo-cleanup-auditor to audit this repository and propose evidence-backed deletions, moves, and renames."
policy:
  allow_implicit_invocation: true
