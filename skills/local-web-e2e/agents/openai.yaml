interface:
  display_name: "Local Web E2E"
  short_description: "Build safe, isolated local browser tests"
  default_prompt: "Use $local-web-e2e to design or debug a safe local browser smoke suite for this application."
policy:
  allow_implicit_invocation: true
