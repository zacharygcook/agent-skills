interface:
  display_name: "Local HTML and PDF Reports"
  short_description: "Create polished, visually verified reports"
  default_prompt: "Use $local-html-pdf-reports to turn these findings into a polished local Markdown, HTML, and PDF report."
policy:
  allow_implicit_invocation: true
